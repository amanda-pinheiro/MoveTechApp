<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="atualizaExercicio.Views.CriarTreino.CriarTreinoPage1"
             Title="CriarTreinoPage1">

    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Colors -->
            <Color x:Key="BgDark">#0F0F10</Color>
            <Color x:Key="CardDark">#1A1A1C</Color>
            <Color x:Key="Accent">#6F3BFF</Color>
            <Color x:Key="AccentDark">#5A2EE6</Color>

            <!-- Text Styles -->
            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="FontFamily" Value="QuicksandRegular"/>
            </Style>

            <Style x:Key="SubLabel" TargetType="Label">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="TextColor" Value="#BBBBBB"/>
                <Setter Property="FontFamily" Value="QuicksandRegular"/>
            </Style>

            <Style x:Key="SmallLabel" TargetType="Label">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="TextColor" Value="#9E9E9E"/>
                <Setter Property="FontFamily" Value="QuicksandLight"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        

        <Grid RowDefinitions="Auto,Auto,*" Padding="16" RowSpacing="16">

            

            <!-- Título da página -->
            <Label Grid.Row="1"
                   Text="Adicionar Exercício"                 
                   FontSize="20"
                   HorizontalOptions="Center"/>

            

            <!-- Área de conteúdo -->
            <ScrollView Grid.Row="2">
                <VerticalStackLayout Spacing="16">

                    <!-- Barra de busca -->
                    <SearchBar x:Name="SearchBarExercicio"
                               Placeholder="Procurar exercício"
                               PlaceholderColor="#CABAEB"
                               BackgroundColor="#2C2C2C"
                               TextColor="#CABAEB"
                               FontFamily="QuicksandRegular"
                               HeightRequest="45"
                               TextChanged="SearchBar_TextChanged"/>

                    <!-- Label: Lista de Exercícios -->
                    <Label Text="Lista de Exercícios"
                           FontSize="14"
                           Margin="0,8,0,0"/>

                    <!-- Lista de exercícios -->
                    <VerticalStackLayout x:Name="ListaExerciciosContainer" 
                                         Spacing="12">
                        <!-- Os exercícios serão adicionados dinamicamente aqui -->
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </ScrollView>

        </Grid>
    </Grid>

</ContentPage>
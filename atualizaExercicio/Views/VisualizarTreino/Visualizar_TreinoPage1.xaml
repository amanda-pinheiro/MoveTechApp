<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:atualizaExercicio.Controls"
             x:Class="atualizaExercicio.Views.VisualizarTreino.Visualizar_TreinoPage1"
             Title="Visualizar_TreinoPage1"
             Background="#010101">

    <ContentPage.Resources>
        <ResourceDictionary>

            <!-- Text Styles -->
            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="FontFamily" Value="QuicksandRegular"/>
            </Style>

            <Style x:Key="SubLabel" TargetType="Label">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="TextColor" Value="#BBBBBB"/>
                <Setter Property="FontFamily" Value="QuicksandRegular"/>
            </Style>

            <Style x:Key="SmallLabel" TargetType="Label">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="TextColor" Value="#9E9E9E"/>
                <Setter Property="FontFamily" Value="QuicksandLight"/>
            </Style>

            <Style TargetType="SearchBar">
                <Setter Property="FontFamily" Value="QuicksandRegular"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="Padding" Value="7"/>
                <Setter Property="BorderWidth" Value="0"/>
                <Setter Property="CharacterSpacing" Value="0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    
        <Grid RowDefinitions="Auto,*,Auto" Padding="0">

            <!-- Header -->
            <Grid Grid.Row="0" HeightRequest="70">

            </Grid>

            <!-- ✅ CORREÇÃO: Adicionado x:Name no ScrollView -->
            <ScrollView x:Name="ScrollViewPrincipal" Grid.Row="1">
                <VerticalStackLayout x:Name="MainContainer" Padding="16" Spacing="18">

                    <!-- Search bar + Novo Treino -->
                    <Grid ColumnDefinitions="Auto,*,Auto" VerticalOptions="Center" ColumnSpacing="8" Padding="0">

                        <!-- ✅ CORREÇÃO: Adicionado TextChanged e x:Name na SearchBar -->
                        <SearchBar x:Name="SearchBarTreinos"
                           Grid.Column="1"
                           Placeholder="Buscar Treino"
                           PlaceholderColor="#CABAEB"
                           BackgroundColor="#2C2C2C"
                           TextColor="#CABAEB"
                           FontFamily="QuicksandRegular"
                           HeightRequest="45"
                           VerticalOptions="Center"
                           TextChanged="SearchBar_TextChanged"/>

                        <!-- Botão "Novo Treino" -->
                        <Grid Grid.Column="2"                             
                              HorizontalOptions="End"
                              VerticalOptions="Center"
                              Margin="5,0,0,0">

                            <!-- ✅ CORREÇÃO: Alterado Command para Clicked -->
                            <ImageButton x:Name="BtnNovoTreino"
                                    Source="addtreino.png"
                                    Background="Transparent"     
                                    CornerRadius="17"
                                    HeightRequest="57"                                   
                                    Padding="0"
                                    Clicked="btn_NovoTreino"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"/>                            
                        </Grid>

                    </Grid>

                    <!-- ✅ CORREÇÃO: Adicionado x:Name no Label para facilitar localização -->
                    <Label x:Name="LabelSeusTreinos"
                           Text="Seus Treinos:" 
               		       Style="{StaticResource SubLabel}" 
               		       FontSize="16"
                           HorizontalOptions="Center"/>

                    <!-- ✅ CORREÇÃO: Container VAZIO - cards serão adicionados dinamicamente -->
                    <VerticalStackLayout x:Name="TreinosContainer" Spacing="12">
                        <!-- ✅ OS CARDS SERÃO ADICIONADOS DINAMICAMENTE NO CODE-BEHIND -->
                    </VerticalStackLayout>

                    <BoxView HeightRequest="30" BackgroundColor="Transparent"/>

                </VerticalStackLayout>
            </ScrollView>
        

        <!-- Menu sobreposto -->
        <controls:MenuHamburguer x:Name="menuHamburguer"
                          Grid.RowSpan="2"
                          VerticalOptions="Start"/>
    </Grid>

</ContentPage>